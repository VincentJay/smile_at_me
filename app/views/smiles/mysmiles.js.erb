$("#grid").append("<%= escape_javascript( render partial: "mysmile", collection: @mysmiles ) %>");
<% if ( @mysmiles.length < 10 ) %>
  //$("#loadMoreMySmilesBtn").html("Bottom of lsit");
  //$("#loadMoreMySmilesBtn").unbind('click');
  $("#loadMoreMySmilesBtn").css("display", "none");   
<% else %>
  $("#loadMoreMySmilesBtn").html("Load More");
<% end %>


//添加刚刚post的图片，刷新home视图，重新排版
new GridScrollFx( document.getElementById( 'grid' ), {
			viewportFactor : 0.4
		} );
//根据浏览器大小自动调整图片宽度
ajustSmileFrameWidth();
//调用点赞函数为ajax获得的内容增加时间函数
favorUnfavor();
//为“smil@”功能添加事件函数
getUploadedSmiles();
