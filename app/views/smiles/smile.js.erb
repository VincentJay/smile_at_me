//创建smile成功，把刚刚创建的smile添加到home界面中
$("#grid").children().first().before('<%= escape_javascript(render partial: "smile", object: @smile) %>');
//添加刚刚post的图片，刷新home视图，重新排版
new GridScrollFx( document.getElementById( 'grid' ), {
			viewportFactor : 0.4
		} );
//调用点赞函数为ajax获得的内容增加时间函数
favorUnfavor();
//为“smil@”功能添加事件函数
getUploadedSmiles();

//关闭模态
//$('#postSmile').modal('toggle');
//根据浏览器大小自动调整图片宽度
ajustSmileFrameWidth();